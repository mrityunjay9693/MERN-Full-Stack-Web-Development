/**
 * length is a property of string in JavaScript is used to find length of the given string i.e.,
 * total number of character inside a string.
 * Syntax : string_variable.length; or "given string".length
 */
let str = "tildetech";
let lengthOfString = str.length;
console.log("Total length of " + str + " is");
console.log(lengthOfString);

let stateName = "Bihar";
console.log(stateName.length);

let len = "Muzaffarpur".length;
console.log(len);

console.log("India".length);