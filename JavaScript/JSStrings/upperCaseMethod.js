/**
 * toUpperCase() method: The toUpperCase() method converts each characters in the given string to its uppercase equivalent
                         transforming the entire string into upper case characters.
 * Syntax: string.toUpperCase();
 */
let myName = "Mrityunjay Kumar";
console.log(myName.toUpperCase()); //MRITYUNJAY KUMAR