/**
 * toLowerCase() method: The toLowerCase() method converts each characters in the given string to its lower case equivalent
                         transforming the entire string into upper case characters. This means the method converts entire
                         characters of given string into lower case i.e., string fully converted to lower case.
 * Syntax: string.toLowerCase();
 */
let greet = "Good Morning, Welcome to TILDE";
console.log(greet.toLowerCase());