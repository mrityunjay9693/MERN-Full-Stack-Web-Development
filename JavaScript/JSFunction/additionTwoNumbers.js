// Addition of two numbers using function addTwoNum by passing two numbers as parameters.

function addTwoNum(a, b) {    // a and b are two parameters of the function which will accpet two numbers as inputs.
    
    console.log(`The addition of a and b = `, a + b );

}
addTwoNum(12,23); // 12, 23 are two arguments  
